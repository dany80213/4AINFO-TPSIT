<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Gallery</title>
  <style>
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 14px; }
    .card { border: 1px solid #ddd; padding: 10px; border-radius: 10px; }
    img { width: 100%; height: 180px; object-fit: cover; border-radius: 8px; }
  </style>
</head>
<body>
  <h1>Post Gallery</h1>

  <p><a href="/post">Crea un nuovo post</a></p>

  <% if (!posts || posts.length === 0) { %>
    <p>Nessun post salvato.</p>
  <% } else { %>
    <div class="grid">
      <% posts.forEach(p => { %>
        <div class="card">
          <a href="/post/<%= p.id %>">
            <% if (p.imagePath) { %>
              <img src="<%= p.imagePath %>" alt="immagine post" />
            <% } else { %>
              <div style="height:180px; display:flex; align-items:center; justify-content:center; background:#f3f3f3; border-radius:8px;">
                <span>Nessuna immagine</span>
              </div>
            <% } %>
          </a>
          <h3><%= p.message %></h3>
          <p><%= p.description %></p>
        </div>
      <% }) %>
    </div>
  <% } %>
</body>
</html>